# Cloudflare Pages headers. Vite fingerprints asset filenames, so they are safe
# to cache forever; HTML must always revalidate so deploys show immediately.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

/index.html
  Cache-Control: public, max-age=0, must-revalidate
